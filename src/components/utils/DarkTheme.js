

import { createTheme, ThemeProvider } from '@ant-design/react-native';

const darkTheme = createTheme({
  // Primary colors
  primaryColor: '#1DA57A',  // Example primary color
  primaryTextColor: '#FFFFFF',  // Example primary text color
  
  // Background colors
  backgroundColor: '#1C1C1C',  // Example background color
  secondaryBackgroundColor: '#121212',  // Example secondary background color

  // Text colors
  textColor: '#FFFFFF',  // Example text color
  secondaryTextColor: '#B0B0B0',  // Example secondary text color

  // Other customizations as needed
});

export default darkTheme;
